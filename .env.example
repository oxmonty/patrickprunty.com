# Upstash Redis, backing the per-post view counts and the unique visitor count.
# Copy the values from the previous site so the existing counts carry over.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
